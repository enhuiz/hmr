# meta
name: MUNIT/mnist
model: MUNIT

# data
data_dir: "data/crohme"
base_size: [300, 300]

# train
device: cuda
epochs: 5
lr: 0.001
beta1: 0.5
beta2: 0.999
batch_size: 16
mean: [0.5]
continued: true

# log
sample_every: 20
display_size: 6